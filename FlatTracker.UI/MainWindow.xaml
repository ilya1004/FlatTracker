<Window x:Class="FlatTracker.UI.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:FlatTracker.UI"
        mc:Ignorable="d"
        Title="FlatTracker" Height="540" Width="1000">
    <Grid Margin="12">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="340" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <Grid Grid.Column="0" Margin="0,0,12,0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0"
                       Text="Уведомления о новых и обновлённых объявлениях" />

            <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,8,0,8">
                <Button Content="Выбрать все" Padding="8,4" Margin="0,0,8,0"
                        Click="SelectAll_Click" />
                <Button Content="Снять все" Padding="8,4"
                        Click="SelectNone_Click" />
            </StackPanel>

            <ListBox Grid.Row="2" x:Name="DistrictsList" HorizontalContentAlignment="Stretch">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <CheckBox Content="{Binding Name}"
                                  IsChecked="{Binding IsSelected, Mode=TwoWay}"
                                  Checked="OnDistrictToggled"
                                  Unchecked="OnDistrictToggled" />
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>

            <TextBlock Grid.Row="3" x:Name="StatusText" Margin="0,8,0,0" TextWrapping="Wrap" />
        </Grid>

        <Grid Grid.Column="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,8">
                <TextBlock Text="Консоль" FontWeight="Bold" VerticalAlignment="Center" />
                <Button Content="Очистить" Padding="8,4" Margin="12,0,0,0"
                        Click="ClearLog_Click" />
            </StackPanel>

            <TextBox Grid.Row="1"
                     x:Name="LogBox"
                     IsReadOnly="True"
                     FontFamily="Consolas"
                     FontSize="12"
                     TextWrapping="NoWrap"
                     VerticalScrollBarVisibility="Auto"
                     HorizontalScrollBarVisibility="Auto"
                     AcceptsReturn="True" />
        </Grid>
    </Grid>
</Window>